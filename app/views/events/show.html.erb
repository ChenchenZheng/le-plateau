<h1>This is the show page</h1>

<h5><%= @event.boardgame.name %></h5>
<%= link_to "Edit", edit_event_path(@event)  %>
<%= link_to "Delete", event_path, method: :delete, data: { confirm: "Are you sure to delete this event?" }  %>
